# Runtime: none for the CLI demo — it runs on the standard library alone.

# Slack front end (slack_app.py):
slack_bolt>=1.18

# Dev / test:
pytest>=8.0

# Later (live mode with a real AI brain instead of the keyword matcher):
# anthropic>=0.40
